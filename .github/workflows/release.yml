name: build-test-release

on:
  push:
    branches:
      - main
      - dev
    tags:
      - 'v*'

jobs:
  run-reusable-build-test-release:
    uses: bdebek-splunk/reusable-build-test-release/.github/workflows/build-reelease.yml@main
    secrets: inherit